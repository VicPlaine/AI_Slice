# AI Slice 项目启动文档（Windows）

> 文档基线：2026-08-25。本文区分“首次安装”和“日常启动”，命令均以当前仓库结构为准。

本文以当前仓库的真实配置为准：PostgreSQL 使用 Docker Compose 的宿主机端口 `5433`，FastAPI 使用 `8001`，Vite 使用 `5173`，默认 AI 服务为阿里云百炼千问。前端监听全部网卡，公司网段访问地址为 <http://192.168.110.221:5173>；后端仍只监听本机回环地址。

## 1. 环境要求

- Windows 10/11
- Docker Desktop（推荐用于 PostgreSQL）
- Python 3.11+
- `uv`
- Node.js `20.19+` 或 `22.12+` 和 npm（Vite 8 要求）
- FFmpeg（大文件本机导出必须；`ffmpeg.exe` 与 `ffprobe.exe`）
- 已开通所需模型的阿里云百炼 API Key

检查命令：

```powershell
docker --version
python --version
uv --version
node --version
npm --version
```

## 2. 准备 FFmpeg

从 <https://ffmpeg.org/download.html#build-windows> 选择 Windows 预编译版本，解压后确认 `bin` 目录包含：

```text
ffmpeg.exe
ffprobe.exe
```

可以把 `bin` 加入系统 PATH，或在 `backend/.env` 中填写绝对路径：

```dotenv
FFMPEG_BIN_DIR=D:/tools/ffmpeg/bin
```

验证：

```powershell
& "D:\tools\ffmpeg\bin\ffmpeg.exe" -version
& "D:\tools\ffmpeg\bin\ffprobe.exe" -version
```

浏览器分析阶段使用 FFmpeg.wasm，不依赖本机 FFmpeg；大文件自动转入本机原生导出链路，因此正式使用仍建议配置。

## 3. 启动 PostgreSQL

```powershell
cd D:\Workspace\Work\AI_Slice\ai-slice
docker compose up -d postgres
docker compose ps
```

项目 Compose 创建：

- 数据库：`ai_slice`
- 用户：`slice`
- 密码：`slice_dev`
- 宿主机端口：`5433`
- 数据卷：`pgdata`

查看数据库日志：

```powershell
docker compose logs postgres
```

不要在已有数据时执行 `docker compose down -v`，该命令会删除数据库卷。

## 4. 配置 `backend/.env`

仅当 `backend/.env` 不存在时执行：

```powershell
cd D:\Workspace\Work\AI_Slice\ai-slice\backend
Copy-Item .env.example .env
```

`.env.example` 是可提交的模板，`.env` 是本机真实配置。运行程序读取 `.env`；API Key 只能填在 `.env`。

如果 `.env` 已存在，不要再次复制覆盖。修改配置键名或新增配置项时，对照 `.env.example` 手工合并。

推荐配置：

```dotenv
DATABASE_URL=postgresql+asyncpg://slice:slice_dev@localhost:5433/ai_slice

# OpenAI 兼容 LLM；当前使用千问
LLM_API_KEY=请填写你自己的百炼_API_Key
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-plus

# 千问录音文件识别；Key 留空时复用 LLM_API_KEY
ASR_PROVIDER=dashscope
DASHSCOPE_API_KEY=
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/api/v1
DASHSCOPE_ASR_MODEL=qwen-audio-3.0-asr-flash-filetrans
DASHSCOPE_ASR_POLL_INTERVAL_SECONDS=3
DASHSCOPE_ASR_TIMEOUT_SECONDS=7200

TEMP_DIR=./tmp
STORAGE_DIR=./storage
WORKER_CONCURRENCY=10
FFMPEG_BIN_DIR=D:/tools/ffmpeg/bin
```

如果你在百炼业务空间里创建了专属应用，应分别使用该空间提供的 OpenAI 兼容地址和原生 DashScope 地址。不要把兼容模式地址填到 `DASHSCOPE_BASE_URL`，也不要把原生 `/api/v1` 地址填到 `LLM_BASE_URL`。同一 Key 还需要同时拥有 LLM 与 ASR 模型权限。`Model.AccessDenied` 通常是 Key、业务空间、地域或模型授权不匹配。

如需临时改用 Groq ASR：

```dotenv
ASR_PROVIDER=groq
GROQ_API_KEY=你的_Groq_Key
GROQ_ASR_MODEL=whisper-large-v3-turbo
GROQ_ASR_CHUNK_MINUTES=25
```

LLM 仍由 `LLM_*` 控制，不受 ASR 供应商切换影响。

## 5. 安装并迁移后端

```powershell
cd D:\Workspace\Work\AI_Slice\ai-slice\backend
uv sync
uv run alembic upgrade head
```

迁移成功后可检查当前版本：

```powershell
uv run alembic current
```

当前 Head 为 `7c9e1a2b3d4f`，其中包含将旧 `interview` 任务迁移为 `podcast` 的数据更新。

启动：

```powershell
uv run uvicorn app.main:app --reload --port 8001
```

验证：

- 健康检查：<http://localhost:8001/api/health>
- Swagger：<http://localhost:8001/docs>

每次修改 `.env` 后都要停止并重新启动后端。

## 6. 安装并启动前端

新开 PowerShell：

```powershell
cd D:\Workspace\Work\AI_Slice\ai-slice\frontend
npm install
npm run dev
```

本机打开 <http://127.0.0.1:5173>，公司网段其他电脑打开 <http://192.168.110.221:5173>。Vite 已配置：

- `/api` → `http://localhost:8001`
- 监听地址：`0.0.0.0:5173`（通过启动脚本传入）
- COOP：`same-origin`
- COEP：`require-corp`

这两个响应头用于启用 FFmpeg.wasm 所需的跨源隔离环境。

## 7. 日常一键启动（推荐）

首次安装、数据库迁移、`.env` 和 `node_modules` 已完成后，在项目根目录运行：

```powershell
cd D:\Workspace\Work\AI_Slice\ai-slice
powershell -ExecutionPolicy Bypass -File .\scripts\start_windows.ps1
```

脚本会：

1. 检查 `uv`、npm、`.env`、`node_modules` 和 PostgreSQL `5433`。
2. 尝试从 `.env` 或仓库常用目录发现 FFmpeg。
3. 停止脚本之前管理的服务，并结束占用 `8001/5173` 的进程。
4. 隐藏启动前后端，PID 写入 `.run/`，日志写入 `logs/`。

需要显示两个服务窗口时增加 `-ShowWindows`：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start_windows.ps1 -ShowWindows
```

脚本不会启动 PostgreSQL、安装依赖或执行 Alembic；拉取包含新迁移的代码后，先手工运行 `uv run alembic upgrade head`。

内网只需开放 TCP 5173。不要把 PostgreSQL 5433 或 FastAPI 8001 暴露给其他电脑；浏览器对 `/api` 的请求由 Vite 在本机代理到后端。

当前 Windows 网络配置为“公用网络”，首次使用内网地址前需以管理员身份打开 PowerShell，并执行一次：

```powershell
New-NetFirewallRule -Name "AI-Slice-LAN-5173" -DisplayName "AI Slice LAN 5173" -Direction Inbound -Action Allow -Protocol TCP -LocalAddress 192.168.110.221 -LocalPort 5173 -RemoteAddress 192.168.108.0/22 -Profile Any
```

规则将入站来源限制为当前公司子网 `192.168.108.0/22`。检查规则：

```powershell
Get-NetFirewallRule -Name "AI-Slice-LAN-5173"
```

## 8. 首次验收

建议先用 1～3 分钟、有人声的小视频验证：

1. 分别确认直播回放、播客访谈和课程讲座三个选项可见。
2. 上传后确认浏览器能够提取并上传音频。
3. 详情页观察转录节点和总进度变化。
4. 任务完成后确认切片卡片、场景标签和东八区时间。
5. 测试标题替换、发布文案替换和剪辑思路。
6. 重新选择同一原视频执行一键切片。
7. 再用大于阈值的视频验证本机原生 FFmpeg 导出。

## 9. 常用命令

```powershell
# 数据库状态
docker compose ps

# 后端测试
cd D:\Workspace\Work\AI_Slice\ai-slice\backend
uv run pytest

# 前端测试和生产构建
cd D:\Workspace\Work\AI_Slice\ai-slice\frontend
node --test tests/*.test.ts
npm run build

# 查看后台启动日志
Get-Content D:\Workspace\Work\AI_Slice\ai-slice\logs\backend.err.log -Tail 100
Get-Content D:\Workspace\Work\AI_Slice\ai-slice\logs\frontend.err.log -Tail 100
```

## 10. 故障排查

### 数据库连接失败

确认容器健康、端口为 `5433`，并核对 `DATABASE_URL`：

```powershell
docker compose ps
Test-NetConnection localhost -Port 5433
```

### `Model.AccessDenied`

检查百炼控制台中的 API Key 所属业务空间、地域与目标模型权限。LLM 和 ASR 是两类调用，只有 LLM 权限的 Key 不一定能调用 ASR。

### `ASR_RESPONSE_HAVE_NO_WORDS`

先播放原视频确认存在人声，再检查浏览器提取出的音频是否为空或静音。纯画面视频不应提交 ASR。

### `Array buffer allocation failed`

这是浏览器 WebAssembly 内存不足。当前代码会按输出体积自动选择原生 FFmpeg；如果仍走浏览器链路，硬刷新前端并确认使用最新代码。大文件原生导出还要求后端重启且 FFmpeg 路径有效。

### 页面连接不上后端

确认 FastAPI 使用 `8001`，而不是 `8000`；再检查 <http://localhost:8001/api/health>。

### 时间显示少 8 小时

数据库保存 UTC 无时区时间，前端统一补 UTC 标记并用 `Asia/Shanghai` 格式化。更新代码后硬刷新页面。

### 仍然看到“面试录像”

确认前端使用最新构建并硬刷新浏览器；随后在 `backend` 目录运行 `uv run alembic upgrade head`。前端兼容层会把旧 `interview` 显示为“播客访谈”，迁移则会永久更新数据库值。

## 11. 停止服务

- 前端、后端窗口按 `Ctrl+C`。
- PostgreSQL：

```powershell
cd D:\Workspace\Work\AI_Slice\ai-slice
docker compose stop postgres
```

如果使用一键启动脚本，先停止它管理的前后端：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start_windows.ps1 -StopOnly
```

`docker compose stop` 保留数据卷，下一次可直接 `docker compose up -d postgres`。
