# ⚠️ 已弃用 — 依赖已迁移至 pyproject.toml，由 uv 管理
# 此文件仅作为参考保留，请勿使用 pip install -r requirements.txt
#
# 使用方法:
#   uv sync                    # 安装依赖

fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
alembic==1.14.0
pydantic-settings==2.5.0
python-dotenv>=1.0.0
openai==1.50.0
pydub==0.25.1
python-multipart==0.0.12
httpx==0.27.0
psycopg[binary]>=3.2
